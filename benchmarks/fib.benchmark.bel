/// Benchmark stub for Fibonacci (will be used by future benchmark harness)

fn fib(n: u64) -> u64 {
    if n <= 1 { n } else { fib(n-1) + fib(n-2) }
}

fn main() {
    let result = fib(30);
    println("fib(30) = {}", result);
    // Future: time this and report
}
